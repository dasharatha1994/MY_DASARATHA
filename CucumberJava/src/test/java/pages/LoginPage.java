package pages;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class LoginPage {
	/*WebDriver driver;

	By txt_username=By.id("email");
	By txt_password=By.id("pass");
	By btn_login=By.xpath("//button[@type='submit']");
	By Your_profile=By.xpath("//*[name()='svg' and @aria-label='Your profile']");
	By Log_Out=By.xpath("//span[contains(text(),'Log Out')]");
	
	public LoginPage(WebDriver driver) {
		this.driver=driver;
		
	}

	public void enterUsername(String username) {
		
		driver.findElement(txt_username).sendKeys(username);
		
	}
public void enterPassword(String password) {
		
		driver.findElement(txt_password).sendKeys(password);
		
	}
public void clickLogin() {
	
	driver.findElement(btn_login).click();
	
}
public void checkLogOutDisplayed() {
	driver.findElement(Your_profile).click();
	
	driver.findElement(Log_Out).click();
}
public void loginValidUser(String username,String password) {
	
	driver.findElement(txt_username).sendKeys(username);
	driver.findElement(txt_password).sendKeys(password);
	driver.findElement(btn_login).click();

  }
}*/



